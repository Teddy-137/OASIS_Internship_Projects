import java.util.Random;

import javax.swing.JOptionPane;

public class Main {
    public static void main(String[] args) {
        new NumberGuessing();

        //In the play() method, the first guess is never assigned to the 'guess' variable
    }
}